{
  "name": "bun-app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot ./src/main.ts",
    "start": "bun ./src/main.ts",
    "build": "bun build ./src/main.ts --outdir ./dist",
    "test": "bun test",
    "docs": "echo '📖 API 文档已启动在 http://localhost:3000/docs' && bun dev"
  },
  "dependencies": {
    "@scalar/api-reference": "^1.31.18",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}